# Базовый URL бэкенда budget-api (NestJS). По умолчанию dev-сервер на 3000.
# Переменные с префиксом VITE_ Vite пробрасывает в клиентский код через import.meta.env.
VITE_API_URL=http://localhost:3000
