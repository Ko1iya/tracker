// Реестр query-ключей TanStack Query для транзакций.
// Централизуем ключи в одном месте, чтобы не дублировать строковые литералы
// по компонентам: useQuery читает по этим ключам, а будущие мутации
// (создание расхода) будут инвалидировать кеш теми же ключами.
export const transactionKeys = {
  all: ["transactions"] as const,
}
